import { Story, Preview } from '@storybook/addon-docs/blocks';

# Button

A `Button` component can serve as a link or a regular button.

It comes in three varieties: _flat (default)_, _filled_ and _outline_.

<Preview>
  <Story id="button--flat" />
  <Story id="button--filled" />
  <Story id="button--outline" />
</Preview>
